# Nginx 配置 - 反向代理 FRP HTTP 服务
# 放置到 /etc/nginx/conf.d/taskbot.conf

server {
    listen 80;
    server_name *.taskbot.com.cn taskbot.com.cn;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 支持（关键！）
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;

        # 大请求体（粘贴场景）
        client_max_body_size 10m;
    }
}

# HTTPS 版本（推荐）
# server {
#     listen 443 ssl;
#     server_name *.taskbot.com.cn;
#
#     ssl_certificate /etc/letsencrypt/live/taskbot.com.cn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/taskbot.com.cn/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_buffering off;
#         proxy_cache off;
#         proxy_read_timeout 86400s;
#     }
# }
